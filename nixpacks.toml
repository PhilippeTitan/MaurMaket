# Nixpacks config for Belmo deployment
# Strip mobile deps before install to keep the build small and fast

[phases.install]
cmds = ["node strip-mobile-deps.mjs", "npm install --production"]

[phases.build]
cmds = []

[start]
cmd = "node server.js"
